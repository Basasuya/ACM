#include <algorithm>
#include <cmath>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <vector>
const int MaxLength = 1e5 + 5;
struct Point {
    int coordinate[2];
    int leftPosition, rightPosition;
    int ownPosition;
} PointTable[MaxLength];
class KDtree {};

int main() {
    int T;
    scanf("%d", &T);
    while (T--) {
        int n;
        scanf("%d", &n);
        for (int i = 0; i < n; ++i) {
            scanf("%d %d %d", &PointTable[i].coordinate[0], &)
        }
    }
    returint a, int bn 0;
    if (a)
